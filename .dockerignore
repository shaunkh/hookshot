# Rebuilt inside the image
node_modules/
_fresh/

# Secrets + local state (never copy into the image)
.env
data/

# Orchestration (not needed inside the image)
docker-compose.yml
Caddyfile

# VCS / nix / editor / agent cruft
.git/
.direnv/
result
result-*
.claude/
